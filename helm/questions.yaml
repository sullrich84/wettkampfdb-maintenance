questions:
  - variable: ingress.enabled
    default: true
    description: "Enable ingress for chart"
    type: boolean
    label: "Enable Ingress"
    group: "Ingress Configuration"
  - variable: ingress.host
    default: "maintenance.wettkampfdb.de"
    description: "The url this service will be accessible at"
    required: true
    type: string
    label: "Ingress Hostname"
    group: "Ingress Configuration"
  - variable: ingress.www
    default: false
    description: "Add additional www host?"
    type: boolean
    label: "Add www host"
    group: "Ingress Configuration"